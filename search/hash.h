#include "../reader/fileControl.h"
#include "../data.h"

//hash init
void hashInit();
//hash
int hash(char* word);
//split
char* split(char* line);
//hashing and insert data
void hashInsert();
// search
void search();
